Third-party licenses for pam-ssh-agent-webauthn
================================================

This document bundles the license texts of every crate statically linked into
the prebuilt `pam_ssh_agent_webauthn-<version>-<arch>.so` artifacts. It is
generated by `cargo about` from `Cargo.lock` at release time; do not edit by
hand.

The crate itself is MIT-licensed (see `LICENSE` in the source tree).

Overview
--------

{{#each overview}}
- {{{name}}} ({{count}})
{{/each}}

----

{{#each licenses}}
## {{{name}}}

Used by:

{{#each used_by}}
- {{{crate.name}}} {{{crate.version}}}{{#if crate.repository}} — {{{crate.repository}}}{{/if}}
{{/each}}

```
{{{text}}}
```

----

{{/each}}
